import matplotlib.pyplot as plt
import PIL.Image

im = PIL.Image.open('butterfly.jpg')
[xs,ys] = im.size
for x in range(0,10):
    for y in range(0,10):
        [r,g,b] = im.getpixel((x,y))
        plt.plot([r],[g],'ro')
print(r, g, b)
